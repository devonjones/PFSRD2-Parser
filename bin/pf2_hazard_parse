#!/usr/bin/env python
import sys

from pfsrd2.hazard import parse_hazard
from universal.options import exec_main, option_parser


def main():
    usage = "usage: %(prog)s [options] [filenames]\nParses hazards from pfsrd2 html to json and writes them to the specified directory"
    parser = option_parser(usage)
    options = parser.parse_args()
    args = options.files
    exec_main(options, args, parse_hazard, "hazards")


if __name__ == "__main__":
    sys.exit(main())
